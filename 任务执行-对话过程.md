# 任务执行-交互对话

## 第一轮：Phase 1
`@./specs/001-core-functionality/plan.md`
`@./specs/001-core-functionality/tasks.md`

请执行 **tasks.md中 Phase 1** 中的所有任务。


## 第二轮：Phase 2
现在进入 **Phase 2**。
请严格遵循TDD流程，**先不要实现功能代码**。

此时 `*.go` 中可能还没有函数实现、或者是空的。请确保测试代码能够通过编译（可以先在 `*.go` 中生成一个空的函数签名），但**运行测试必须失败**。


## 第三轮：Phase 2
测试已失败，正如预期。
现在实现 parser.go 和 fetcher.go。

**要求：**
1.  逻辑必须能通过刚才编写的所有测试用例。
2.  使用字符串分割（`strings.Split`）来解析URL，**遵循“简单性原则”，尽量避免复杂的正则表达式**。
3.  严格遵循 `spec.md` 中定义的URL模式。
4.  不要过度设计，只写能通过测试的代码。


## 第四轮：Phase 3
现在进入 **Phase 3**。
请严格遵循TDD流程，**先不要实现功能代码**。

请确保测试代码能够通过编译（可以先生成一个空的函数签名），但**运行测试必须失败**。


## 第五轮：Phase 3
测试已失败，正如预期。
现在实现 formatter.go 和 converter.go。

**要求：**
1.  逻辑必须能通过刚才编写的所有测试用例。
2.  不要过度设计，只写能通过测试的代码。


## 第六轮：Phase 4
`@./specs/001-core-functionality/plan.md`
`@./specs/001-core-functionality/tasks.md`

请执行 **tasks.md中 Phase 4** 中的所有任务。










